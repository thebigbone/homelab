apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus-route
spec:
  parentRefs:
    - name: envoy-gateway
  hostnames:
    - "gatus.lan"
  rules:
    - backendRefs:
        - name: gatus
          port: 8080
