apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth

resources:
  - namespace.yaml
  - pvc.yaml
  - sa.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ./routes

commonLabels:
  app.kubernetes.io/component: pocket-id
