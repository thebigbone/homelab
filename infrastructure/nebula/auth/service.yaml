apiVersion: v1
kind: Service
metadata:
  name: pocket-id
  labels:
    app.kubernetes.io/component: pocket-id
spec:
  selector:
    app.kubernetes.io/component: pocket-id
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 1411
