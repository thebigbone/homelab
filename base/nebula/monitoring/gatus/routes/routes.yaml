apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus-route
spec:
  parentRefs:
    - name: envoy-gateway
  hostnames:
    - "gatus.ext4.xyz"
  rules:
    - backendRefs:
        - name: gatus
          port: 8080
